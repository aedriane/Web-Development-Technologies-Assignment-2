@model IEnumerable<Assignment2.Models.SessionTime>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SessionTime1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SessionTime2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SessionTime1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SessionTime2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SessionTimeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SessionTimeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SessionTimeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
